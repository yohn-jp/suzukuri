name: Publish to npm

on:
  release:
    types: [published]

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    uses: yohn-jp/.github/.github/workflows/npm-publish.yml@9962f9262e333e4d3b14c5d5fe13a0b012c2d0bb
    with:
      working-directory: .
      node-version: "22"
      smoke-test-node-versions: "22,24"
      npm-cli-version: "11.5.1"
